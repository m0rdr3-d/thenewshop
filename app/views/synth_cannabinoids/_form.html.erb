<%= form_with(model: synth_cannabinoid, local: true) do |form| %>
  <% if synth_cannabinoid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(synth_cannabinoid.errors.count, "error") %> prohibited this synth_cannabinoid from being saved:</h2>

      <ul>
      <% synth_cannabinoid.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :avatar %>
    <%= form.text_field :avatar %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
